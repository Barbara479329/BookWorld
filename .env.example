# Copier ce fichier en .env (non versionné) et renseigner un token secret,
# ou exporter directement la variable dans le shell avant de lancer l'API :
#   export API_TOKEN="mon-token-secret"
API_TOKEN=change-me-before-running-in-anything-but-dev
